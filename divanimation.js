// Variable to control the speed of the fade out animation
        var fadeOutSpeed = 1; // Speed in seconds

        // Apply the fade out speed to the animation
        document.getElementById('intro-overlay').style.animationDuration = fadeOutSpeed + 's';
